---
{
  "source_id": 37,
  "url": "https://www.chinago.world?spm=..index.header_1.1&spm_prev=..page_2385077.header_1.1",
  "source_domain": "chinago.world",
  "source_type": "unknown",
  "reliability_label": "low",
  "title": "跨境动态",
  "fetched_at": "2026-05-20T21:46:14.065187+00:00",
  "extraction_method": "tavily_extract",
  "tavily_request_id": "8e6676eb-3267-42b4-ba92-963e515702eb",
  "tavily_response_time": 25.98,
  "tavily_usage": {
    "credits": 1
  },
  "favicon": "https://img.staticdj.com/7d876254b2b34822c5060eb8fd0e9e0f.png",
  "images": []
}
---

# 跨境动态

# 

* [首页](https://www.chinago.world/)
* [关于协会](/pages/关于协会) 

  + [会长致辞](/pages/2022跨境电商20时代开启新征程)
  + [协会概况](/pages/深跨协简介)
  + [组织架构](https://www.chinago.world/pages/%E7%BB%84%E7%BB%87%E6%9E%B6%E6%9E%84?st=)
  + [联系我们](/pages/联系我们)
  + [领导班子](/pages/67x)
  + [通知公告](/blogs/通知公告)
  + [协会活动](/blogs/协会活动)
  + [人才招聘](/pages/人才招聘)
* [会员服务](/pages/会员服务) 

  + [入会指南](/pages/入会须知)
  + [会员管理](/pages/会员管理1)
  + [服务类别](/pages/服务支持与保障)
* [跨境动态](/blogs/行业新闻) 

  + [会员动态](/blogs/会员动态)
  + [跨境人物](/blogs/跨境人物)
* [会展服务](/) 

  + [展会预告](/pages/展会预告)
  + [申展指南](/pages/申展指南1)
  + [往届风采](/pages/往届风采1)
  + [历届展商](/pages/历届展商)
* + [专家委员会](/pages/研究院专家委员会)
  + [法律委员会](/pages/法律委员会)
  + [物流委员会](/pages/物流委员会)
* 产业带供应链

  + [K100B2B](/pages/k100b2b)
  + [产品展示中心](/pages/产品展示中心)
  + [产业带活动](/pages/产业带活动)
* [人才招聘](/pages/人才招聘)
* 更多链接

[登录](/account/login) [创建账号](/account/register)

 [${(data.data && data.data.count) > 999 ? '999+' : (data.data && data.data.count)}](/cart)

# 

[${(data.data && data.data.count) > 999 ? '999+' : (data.data && data.data.count)}](/cart)

* [首页](https://www.chinago.world/)
* [关于协会](/pages/关于协会)

  关于协会

  + [会长致辞](/pages/2022跨境电商20时代开启新征程)
  + [协会概况](/pages/深跨协简介)
  + [组织架构](https://www.chinago.world/pages/%E7%BB%84%E7%BB%87%E6%9E%B6%E6%9E%84?st=)
  + [联系我们](/pages/联系我们)
  + [领导班子](/pages/67x)
  + [通知公告](/blogs/通知公告)
  + [协会活动](/blogs/协会活动)
  + [人才招聘](/pages/人才招聘)
* [会员服务](/pages/会员服务)

  会员服务

  + [入会指南](/pages/入会须知)
  + 会员权益
  + [会员管理](/pages/会员管理1)
  + [服务类别](/pages/服务支持与保障)
* [跨境动态](/blogs/行业新闻)

  跨境动态

  + [会员动态](/blogs/会员动态)
  + [跨境人物](/blogs/跨境人物)
  + 行业动态
  + 公告通知
  + 法律资讯
* [会展服务](/)

  会展服务

  + [展会预告](/pages/展会预告)
  + [申展指南](/pages/申展指南1)
  + [往届风采](/pages/往届风采1)
  + [历届展商](/pages/历届展商)
* 标准委员会

  标准委员会

  + [专家委员会](/pages/研究院专家委员会)
  + [法律委员会](/pages/法律委员会)
  + [物流委员会](/pages/物流委员会)
* 产业带供应链

  产业带供应链

  + [K100B2B](/pages/k100b2b)
  + [产品展示中心](/pages/产品展示中心)
  + [产业带活动](/pages/产业带活动)
* [人才招聘](/pages/人才招聘)

[登录](/account/login)

[点击报名](https://www.hudongba.com/dis/94sqx06w38)

 

${Array(data.total || 0).fill().map((val, idx) => idx).map((val, idx) => ` `).join('')} 

[点击报名](https://www.hudongba.com/dis/94sqx06w38)

 

${Array(data.total || 0).fill().map((val, idx) => idx).map((val, idx) => ` `).join('')}

${Array(data.total || 0).fill().map((val, idx) => idx).map((val, idx) => ` `).join('')}  

${Array(data.total || 0).fill().map((val, idx) => idx).map((val, idx) => ` `).join('')}

### 跨境动态

跨境人物

深圳市跨境电子商务协会 •  ${data.agoText}

王馨会长接受《香港经济导报》新年首刊专访：跨境电商更需搭桥

作为外贸增长的新引擎，更是互联网时代最朴实的民间经济外交，以一个个跨境包裹、一次次跨境交易打破国家间壁垒，让世界市场更紧密相连。日前，深圳市跨境电子商务协会（下称“深跨协”）会长王馨女士接受香港经济导报首席记者梁帆马年第一刊专访，探讨跨境电商新一年的挑战与机遇。以下是专访全文：

协会要闻

深圳市跨境电子商务协会 •  ${data.agoText}

2026王馨会长跨年说：从全球货架到共生文明｜全文

1月15日，值此辞旧迎新、岁序更迭之际，由深圳市跨境电子商务协会举办的2026跨境电商行业数据大会在深圳爆场举行，而今年的「2026跨年说」演讲也如期而至。众多知名跨境电商企业、相关方及业内大咖前来参会，共同回溯中国跨境电商一路走来的发展脉络，解读市场风云变幻的密码，剖析当下格局背后潜藏的深层逻辑。

行业新闻

深圳市跨境电子商务协会 •  ${data.agoText}

王馨会长：左手货源右手全球，“超级心脏”郑州构筑开放新高地

联系我们

联系我们

协会地址

广东省深圳市南山区留仙大道1195号匠造数字产业园A栋西座5楼502

全国服务热线

0755-23309910

©  深圳市跨境电子商务协会 Copyright szcbea all rights reserved.©2021

${function() { const force\_image\_size = "natural"; const product\_hover\_on = false; const product\_show\_sale\_label = false; const product\_save\_type = "amount"; const badge1 = "New"; const badge2 = ""; const badge3 = ""; const product\_title\_mobile\_hide = false; const product\_title\_style = "full"; const enable\_collection\_thumb\_image = false; const variantNames = ["color"]; const thumbMaxNum = 4; const suffix\_id = null || ''; const url = data.url; const productId = data.id; const price = data.price; const productAvailable = data.available; const productVariants = data.variants || []; const images = data.images || []; const image = data.image || {}; const imageWidth = image.width; let imageHeight = image.height; let image2 = null; let imageHoverOn = false; if (force\_image\_size !== 'natural') { imageHeight = imageWidth \* force\_image\_size; } if (product\_hover\_on) { for (let i = 1; i < images.length; i++) { const img = images[i]; if (img.src && img.src.indexOf('video=') === -1) { image2 = img; imageHoverOn = true; break; } } } let compareAtPrice = data.compare\_at\_price; let offRatio = data.off\_ratio; let discountMaxUrl = url; const showFrom = data.price\_min != data.price\_max ? true : false; for (let i = 0; i < productVariants.length; i++) { const variant = productVariants[i]; if (variant.price == price && compareAtPrice < variant.compare\_at\_price) { compareAtPrice = variant.compare\_at\_price; offRatio = variant.off\_ratio; variant.available && (discountMaxUrl = variant.url); } } const soldOutText = "\u552e\u7a7a"; let soldOn = false; let saleOn = false; if (productAvailable) { if (product\_show\_sale\_label && price < compareAtPrice) { saleOn = true; } } else { soldOn = true; } const diffPrice = compareAtPrice - price; const saveLabelContent = product\_save\_type == 'percent' ? (offRatio + '%') : ``; let saveText = "\u4f18\u60e0{off\_ratio}"; saveText = saveText.replace('{off\_ratio}', saveLabelContent); const productTitle = data.title; const heading = "title" === 'subtitle' ? data.brief : productTitle; const variantValues = []; const showVariants = []; if (data.need\_variant\_image && enable\_collection\_thumb\_image && variantNames.length > 0) { for (let i = 0; i < (data.options || []).length; i++) { const option = data.options[i]; const optionName = option.name.toLowerCase(); if (variantNames.includes(optionName)) { for (let j = 0; j < productVariants.length; j++) { const variant = productVariants[j]; const value = variant.options[i].value; if (!variantValues.includes(value)) { variantValues.push(value); showVariants.push(variant); } } break; } } } return `

[${showVariants.map(item => { return ` `; }).join('')}](${url}) 

[${saleOn ? saveText : soldOutText}](${discountMaxUrl})  ${badge1} ${badge2} ${badge3}

## [${heading}](${url})

 [从](${discountMaxUrl})   ${showVariants.map((item, idx) => { let showMoreHtml = ''; if (idx == thumbMaxNum) { showMoreHtml += ` `; } return ` ${showMoreHtml}  `; }).join('') }

`; }()}

### 你可能还喜欢

 ${data.products && data.products.map(data => `

${function() { const force\_image\_size = "natural"; const product\_hover\_on = false; const product\_show\_sale\_label = false; const product\_save\_type = "amount"; const badge1 = "New"; const badge2 = ""; const badge3 = ""; const product\_title\_mobile\_hide = false; const product\_title\_style = "full"; const enable\_collection\_thumb\_image = false; const variantNames = ["color"]; const thumbMaxNum = 4; const suffix\_id = "cart-recommendations-pc" || ''; const url = data.url; const productId = data.id; const price = data.price; const productAvailable = data.available; const productVariants = data.variants || []; const images = data.images || []; const image = data.image || {}; const imageWidth = image.width; let imageHeight = image.height; let image2 = null; let imageHoverOn = false; if (force\_image\_size !== 'natural') { imageHeight = imageWidth \* force\_image\_size; } if (product\_hover\_on) { for (let i = 1; i < images.length; i++) { const img = images[i]; if (img.src && img.src.indexOf('video=') === -1) { image2 = img; imageHoverOn = true; break; } } } let compareAtPrice = data.compare\_at\_price; let offRatio = data.off\_ratio; let discountMaxUrl = url; const showFrom = data.price\_min != data.price\_max ? true : false; for (let i = 0; i < productVariants.length; i++) { const variant = productVariants[i]; if (variant.price == price && compareAtPrice < variant.compare\_at\_price) { compareAtPrice = variant.compare\_at\_price; offRatio = variant.off\_ratio; variant.available && (discountMaxUrl = variant.url); } } const soldOutText = "\u552e\u7a7a"; let soldOn = false; let saleOn = false; if (productAvailable) { if (product\_show\_sale\_label && price < compareAtPrice) { saleOn = true; } } else { soldOn = true; } const diffPrice = compareAtPrice - price; const saveLabelContent = product\_save\_type == 'percent' ? (offRatio + '%') : ``; let saveText = "\u4f18\u60e0{off\_ratio}"; saveText = saveText.replace('{off\_ratio}', saveLabelContent); const productTitle = data.title; const heading = "title" === 'subtitle' ? data.brief : productTitle; const variantValues = []; const showVariants = []; if (data.need\_variant\_image && enable\_collection\_thumb\_image && variantNames.length > 0) { for (let i = 0; i < (data.options || []).length; i++) { const option = data.options[i]; const optionName = option.name.toLowerCase(); if (variantNames.includes(optionName)) { for (let j = 0; j < productVariants.length; j++) { const variant = productVariants[j]; const value = variant.options[i].value; if (!variantValues.includes(value)) { variantValues.push(value); showVariants.push(variant); } } break; } } } return `

[${showVariants.map(item => { return ` `; }).join('')}](${url}) 

[${saleOn ? saveText : soldOutText}](${discountMaxUrl})  ${badge1} ${badge2} ${badge3}

## [${heading}](${url})

 [从](${discountMaxUrl})   ${showVariants.map((item, idx) => { let showMoreHtml = ''; if (idx == thumbMaxNum) { showMoreHtml += ` `; } return ` ${showMoreHtml}  `; }).join('') }

`; }()}

`).join('')} 

${data.products && data.products.map(data => `

${function() { const force\_image\_size = "natural"; const product\_hover\_on = false; const product\_show\_sale\_label = false; const product\_save\_type = "amount"; const badge1 = "New"; const badge2 = ""; const badge3 = ""; const product\_title\_mobile\_hide = false; const product\_title\_style = "full"; const enable\_collection\_thumb\_image = false; const variantNames = ["color"]; const thumbMaxNum = 4; const suffix\_id = "cart-recommendations-mobile" || ''; const url = data.url; const productId = data.id; const price = data.price; const productAvailable = data.available; const productVariants = data.variants || []; const images = data.images || []; const image = data.image || {}; const imageWidth = image.width; let imageHeight = image.height; let image2 = null; let imageHoverOn = false; if (force\_image\_size !== 'natural') { imageHeight = imageWidth \* force\_image\_size; } if (product\_hover\_on) { for (let i = 1; i < images.length; i++) { const img = images[i]; if (img.src && img.src.indexOf('video=') === -1) { image2 = img; imageHoverOn = true; break; } } } let compareAtPrice = data.compare\_at\_price; let offRatio = data.off\_ratio; let discountMaxUrl = url; const showFrom = data.price\_min != data.price\_max ? true : false; for (let i = 0; i < productVariants.length; i++) { const variant = productVariants[i]; if (variant.price == price && compareAtPrice < variant.compare\_at\_price) { compareAtPrice = variant.compare\_at\_price; offRatio = variant.off\_ratio; variant.available && (discountMaxUrl = variant.url); } } const soldOutText = "\u552e\u7a7a"; let soldOn = false; let saleOn = false; if (productAvailable) { if (product\_show\_sale\_label && price < compareAtPrice) { saleOn = true; } } else { soldOn = true; } const diffPrice = compareAtPrice - price; const saveLabelContent = product\_save\_type == 'percent' ? (offRatio + '%') : ``; let saveText = "\u4f18\u60e0{off\_ratio}"; saveText = saveText.replace('{off\_ratio}', saveLabelContent); const productTitle = data.title; const heading = "title" === 'subtitle' ? data.brief : productTitle; const variantValues = []; const showVariants = []; if (data.need\_variant\_image && enable\_collection\_thumb\_image && variantNames.length > 0) { for (let i = 0; i < (data.options || []).length; i++) { const option = data.options[i]; const optionName = option.name.toLowerCase(); if (variantNames.includes(optionName)) { for (let j = 0; j < productVariants.length; j++) { const variant = productVariants[j]; const value = variant.options[i].value; if (!variantValues.includes(value)) { variantValues.push(value); showVariants.push(variant); } } break; } } } return `

[${showVariants.map(item => { return ` `; }).join('')}](${url}) 

[${saleOn ? saveText : soldOutText}](${discountMaxUrl})  ${badge1} ${badge2} ${badge3}

## [${heading}](${url})

 [从](${discountMaxUrl})   ${showVariants.map((item, idx) => { let showMoreHtml = ''; if (idx == thumbMaxNum) { showMoreHtml += ` `; } return ` ${showMoreHtml}  `; }).join('') }

`; }()}

`).join('')}

${ data == 1 ? ("\u4f60\u7684\u8d2d\u7269\u8f66\u5df2\u52a0\u51651\u4e2a\u5546\u54c1") : ("\u4f60\u7684\u8d2d\u7269\u8f66\u5df2\u52a0\u5165{count}\u4e2a\u5546\u54c1").replace('{count}', data) }

${data.product.title}

${data.variant.options.map(option => option.value).join(' / ')}

${data.data.item\_count == 1 ? ("\u8d2d\u7269\u8f66\u5c0f\u8ba1(1\u4e2a\u5546\u54c1\uff09") : ("\u8d2d\u7269\u8f66\u5c0f\u8ba1\uff08{count} \u4e2a\u5546\u54c1\uff09").replace('{count}', data.data.item\_count)}

${("\u4f18\u60e0{off\_ratio}").replace('{off\_ratio}', ``)}

税费和运费将在结账时计算

[查看购物车](/cart) [继续购物](/)
